{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "University Schema",
  "description": "JSON Schema for a university entity.",
  "type": "object",
  "properties": {
    "name": {
      "type": "object",
      "description": "Localized name of the university",
      "properties": {
        "ru": { "type": "string" },
        "kz": { "type": "string" },
        "en": { "type": "string" }
      },
      "required": ["ru", "kz"]
    },
    "abbr": {
      "type": "string",
      "description": "University abbreviation"
    },
    "city": {
      "type": "string",
      "description": "City where the university is located"
    },
    "type": {
      "type": "string",
      "description": "Type of the university (e.g., public, private, national)"
    },
    "website": {
      "type": "string",
      "format": "uri",
      "description": "Official website URL"
    },
    "description": {
      "type": "string",
      "description": "Brief description of the university"
    },
    "logo_url": {
      "type": ["string", "null"],
      "format": "uri",
      "description": "URL to the university's logo"
    },
    "specialty_groups": {
      "type": "array",
      "description": "List of educational program groups (ГОП) with ENT score requirements",
      "items": {
        "type": "object",
        "properties": {
          "group_code": {
            "type": "string",
            "description": "Code of the educational program group"
          },
          "min_score": {
            "type": "integer",
            "description": "Minimum ENT score required to apply"
          },
          "last_year_threshold": {
            "type": "integer",
            "description": "Passing ENT score from the previous year"
          }
        },
        "required": [
          "group_code",
          "min_score",
          "last_year_threshold"
        ]
      }
    },
    "specialties": {
      "type": "array",
      "description": "List of specific specialties (ОП) offered by the university",
      "items": {
        "type": "string"
      }
    },
    "metadata": {
      "type": "object",
      "description": "Information about data collection and source",
      "properties": {
        "schema_version": { "type": "string" },
        "source_system": { "type": "string" },
        "source_urls": {
          "type": "array",
          "items": { "type": "string", "format": "uri" }
        },
        "collected_at": { "type": "string", "format": "date-time" },
        "notes": { "type": "string" }
      }
    }
  },
  "required": [
    "name",
    "abbr",
    "city",
    "type",
    "website",
    "description",
    "specialty_groups",
    "specialties"
  ]
}